#include "event_macros.h"

/* Script id 402 — system/UI TEXT (no portrait). No sprite FSM in this stream. Talk ROM 0x080739F6. */
EVENT_SCRIPT_REPLACEMENT(0x080739F6, scene_0739F6)

  TEXT("You got the WARP TOOL")
  END()

END_EVENT_SCRIPT()
